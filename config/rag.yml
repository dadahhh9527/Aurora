chat_model_name: qwen3-max
embedding_model_name: text-embedding-v4

# OpenAI 兼容接入（阿里云百炼 compatible-mode）
# 敏感信息从环境变量 / .env 读取，不要写死在此文件中
api_key: ${DASHSCOPE_API_KEY}
base_url: ${DASHSCOPE_BASE_URL:-https://ws-24nvgyct3cn7ppff.cn-beijing.maas.aliyuncs.com/compatible-mode/v1}
