external_data_path: data/external/records.csv

# Weather via OpenWeatherMap (API key read from OPENWEATHER_API_KEY in the environment / .env)
openweather_base_url: https://api.openweathermap.org
weather_timeout: 5

# Geolocation via ip-api.com (free, no API key required)
location_timeout: 5
